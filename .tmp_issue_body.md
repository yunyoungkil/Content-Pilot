## 버그 설명
- 로그인 -> 로그아웃 -> 재로그인 이후 채널 설정 모달에서 이미 등록된 채널의 URL을 입력하면, 기존 채널을 업데이트하지 않고 같은 URL의 채널이 새로 하나 더 추가됩니다.

## 재현 방법
1. 채널을 2개 이상 등록합니다. (예: 채널 A, 채널 B)
2. 채널 B로 전환 후 Google 로그아웃(연동 해제)
3. 다시 로그인
4. 채널 모달에서 이미 등록된 채널과 동일한 URL을 입력하고 저장

## 예상 동작
- 동일한 URL이면 기존 채널을 업데이트하거나 확인창을 통해 업데이트를 허용해야 합니다.

## 실제 동작
- 동일 URL이 새로 추가되어 중복 발생

## 발생 환경
- Chrome 확장(Manifest V3), Firebase Realtime DB, chrome.storage.local 사용

## 참고 로그
- [ChannelMode] Google 로그아웃 - 채널 정보 완전 초기화
- [ChannelMode] activeChannelId 초기화 완료
- [ChannelMode] 로그인 후 채널 데이터 로드 시작
- [ChannelMode] 모달이 열려있음 - 첫 번째 채널 정보 표시
