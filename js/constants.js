// 상수 관리 파일
// 예: 모드, 이벤트 이름, CSS 클래스명 등
const MODES = {
  SCRAPBOOK: 'scrapbook',
  KANBAN: 'kanban',
  DRAFT: 'draft',
};

const STORAGE_KEYS = {
  PANEL_LAYOUT: 'cp_panel_layout',
};

const CLASSNAMES = {
  SCRAP_CARD: 'scrap-card',
  SCRAP_CARD_DELETE_BTN: 'scrap-card-delete-btn',
};
window.MODES = MODES;
window.STORAGE_KEYS = STORAGE_KEYS;
window.CLASSNAMES = CLASSNAMES;
