# 검색어 메트릭 검증 TODO

## 📋 개요
검색어 메트릭이 성과 대시보드에 정상적으로 표시되는지 확인하기 위한 체크리스트입니다.

## ✅ 현재 구현 상태

### 1. 백엔드 (background.js)
- ✅ GA4 API에서 `searchTerm` dimension으로 검색어 수집
- ✅ 검색어 데이터 파싱 및 `topSearchTerms` 배열 생성
- ✅ `analyticsData`에 `topSearchTerms` 포함
- ✅ `performanceData`에 `topSearchTerms` 포함
- ✅ Firebase에 검색어 데이터 저장
- ✅ 검색어 수집 과정 로그 출력

### 2. 프론트엔드 (js/ui/performanceDashboardMode.js)
- ✅ 성과 카드에 검색어 섹션 표시 조건 추가
- ✅ 검색어 UI 렌더링 구현 (상위 3개 검색어 표시)
- ✅ 검색어가 있을 때만 "🔍 검색어" 섹션 표시

## 🔍 검증 체크리스트

### 1단계: 데이터 수집 확인
- [ ] 성과 대시보드 새로고침
- [ ] 개발자 도구 콘솔(F12) 열기
- [ ] `[검색어 메트릭]` 로그 확인
  - [ ] `[검색어 메트릭] API 응답 상태` - 성공 여부 확인
  - [ ] `[검색어 메트릭] 검색어 수집 완료` - 수집된 검색어 개수 확인
  - [ ] `[검색어 메트릭] analyticsData에 검색어 포함 확인` - analyticsData에 포함 여부 확인
  - [ ] `[검색어 메트릭] performanceData에 검색어 포함 확인` - performanceData에 포함 여부 확인

### 2단계: Firebase 저장 확인
- [ ] Firebase 콘솔에서 해당 카드의 `performance` 객체 확인
- [ ] `topSearchTerms` 필드가 존재하는지 확인
- [ ] `topSearchTerms` 배열에 검색어 데이터가 있는지 확인

### 3단계: UI 표시 확인
- [ ] 성과 대시보드에서 해당 카드 확인
- [ ] "🔍 검색어" 섹션이 표시되는지 확인
- [ ] 검색어와 사용자 수가 올바르게 표시되는지 확인
- [ ] 상위 3개 검색어만 표시되는지 확인

## ⚠️ 현재 알려진 이슈

### 검색어 데이터가 없는 경우
- **현상**: API 응답은 성공하지만 검색어 값이 빈 문자열(`""`)로 들어옴
- **원인**: 
  1. 해당 페이지로 검색어를 통해 유입된 트래픽이 없음
  2. URL 쿼리 파라미터(`?q=검색어`)로 검색어를 추적하지 않음
  3. GA4에서 `searchTerm` 이벤트 매개변수를 사용하지 않음
- **해결**: 검색어로 유입된 트래픽이 생기면 자동으로 표시됨

## 📝 검증 시 확인 사항

### 검색어가 표시되지 않는 경우
1. **콘솔 로그 확인**
   - `[검색어 메트릭] API 응답 상태`에서 `rowsCount` 확인
   - `[검색어 메트릭] 필터링된 검색어` 로그 확인 (빈 문자열 필터링 여부)
   
2. **Firebase 데이터 확인**
   - `performance.topSearchTerms` 필드 존재 여부
   - `performance.topSearchTerms.length` 확인
   
3. **UI 조건 확인**
   - `perf.topSearchTerms && perf.topSearchTerms.length > 0` 조건 만족 여부

### 검색어가 표시되는 경우
- ✅ 검색어 섹션이 성과 카드에 표시됨
- ✅ 상위 3개 검색어가 표시됨
- ✅ 각 검색어 옆에 사용자 수가 표시됨
- ✅ 검색어 배지에 호버 시 상세 정보 표시

## 🔄 다음 확인 시점
- **예상 시점**: 검색어로 유입된 트래픽이 발생한 후 (몇 일 후)
- **확인 방법**: 
  1. 성과 대시보드 새로고침
  2. 콘솔에서 `[검색어 메트릭]` 로그 확인
  3. UI에서 검색어 섹션 표시 여부 확인

## 📌 참고사항
- 검색어는 GA4의 `searchTerm` dimension을 사용하여 수집됩니다
- 검색어는 URL 쿼리 파라미터(`?q=검색어`) 또는 `search_term` 이벤트 매개변수로 추적됩니다
- 검색어가 빈 문자열이거나 `(not set)`인 경우 필터링되어 표시되지 않습니다
- 상위 5개 검색어를 수집하지만, UI에는 상위 3개만 표시됩니다

