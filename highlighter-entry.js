// highlighter-entry.js (수정 후)

// 이 스크립트가 실행되면 콘솔에 메시지를 남깁니다.
console.log("✅ Highlighter Script Loaded in Frame:", window.location.href);

import { setupHighlighter } from './js/core/highlighter.js';

setupHighlighter();